apiVersion: apps/v1
kind: Deployment
metadata:
  name: cms-svc
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      app: cms-svc
  template:
    metadata:
      labels:
        app: cms-svc
    spec:
      containers:
      - name: cms-svc
        image: seongjumoon/cms-service:log
        imagePullPolicy: Always
        resources:
          requests:
            memory: "256Mi"
            cpu: "128m"
          limits:
            memory: "256Mi"
            cpu: "128m"
        env:
        - name: PORT
          value: "8082"
        - name: REDIS_HOST
          value: redis.{{- .Release.Namespace -}}.svc.cluster.local
        - name: REDIS_PORT
          value: "6379"
        ports:
        - containerPort: 8082